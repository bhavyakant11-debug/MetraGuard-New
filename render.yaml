services:
  - type: web
    name: metaguard-new
    runtime: python
    rootDir: backend
    buildCommand: apt-get update && apt-get install -y tesseract-ocr && pip install -r requirements.txtssssss
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT